# __GAME_NAME__

Starter game created from the `_template`.

## Run

```bash
npm run dev -- __GAME_NAME__
```

## Build

```bash
npm run build -- __GAME_NAME__
```

## Tips

- Change colors, text, and shapes in `src/main.js`.
- Import helpers from `@mygames/shared` (canvas, input, drawing, math).
- Copy ideas from other games in `games/`.
